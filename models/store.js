// Definindo a classe Store
class Store {
    constructor(
        id, 
        name, 
        city, 
        address
    ) {
        this.id = id
        this.name = name
        this.city = city
        this.address = address
    }
}

module.exports = Store
